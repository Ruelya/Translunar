{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ProtocolCatalog",
  "description": "Root schema exported for the TypeScript contracts package.\n\nThe runtime stdout frame is `EngineFrame` (an internally tagged enum); its\nconstituent parts are exported here individually so the generated\nTypeScript keeps their full field lists.",
  "type": "object",
  "properties": {
    "error": {
      "$ref": "#/$defs/RpcError"
    },
    "methods": {
      "$ref": "#/$defs/RpcMethodCatalog"
    },
    "notification": {
      "$ref": "#/$defs/RpcNotification"
    },
    "notifications": {
      "$ref": "#/$defs/NotificationCatalog"
    },
    "request": {
      "$ref": "#/$defs/RpcRequest"
    },
    "response": {
      "$ref": "#/$defs/RpcResponse"
    }
  },
  "additionalProperties": false,
  "required": [
    "request",
    "response",
    "notification",
    "error",
    "methods",
    "notifications"
  ],
  "$defs": {
    "AgentApprovalMode": {
      "description": "Approval tier for one agent run. The tiers govern what happens to\nAI-generated drafts only: exact-TM reuse, the tag-integrity gate, the\nlocked/confirmed shields, and human-only export are identical everywhere.",
      "oneOf": [
        {
          "description": "Default. AI drafts queue as proposals; nothing is written until a\nhuman approves them through `ai.agent.review`.",
          "type": "string",
          "const": "manual"
        },
        {
          "description": "AI drafts that pass the tag gate land in the grid as drafts.\nConfirmation and export stay human.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Explicit high-trust tier: drafts land like `auto`, and segments whose\nsegment-scoped QA has zero open error-severity issues are confirmed\nthrough the ordinary `segment.confirm` path (TM write included).\nExport stays human; the run still parks at `awaitingReview`.",
          "type": "string",
          "const": "turbo"
        }
      ]
    },
    "AgentCancelParams": {
      "type": "object",
      "properties": {
        "runId": {
          "type": "string"
        }
      },
      "required": [
        "runId"
      ]
    },
    "AgentProposal": {
      "type": "object",
      "properties": {
        "draftTarget": {
          "type": "string"
        },
        "elapsedMs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "model": {
          "type": "string"
        },
        "note": {
          "description": "Present for `stale` proposals: why the live row refused the apply.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        },
        "segmentId": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/AgentProposalStatus"
        },
        "tagCheck": {
          "$ref": "#/$defs/TagIntegrityReport"
        }
      },
      "required": [
        "segmentId",
        "sourceText",
        "draftTarget",
        "provider",
        "model",
        "elapsedMs",
        "tagCheck",
        "status"
      ]
    },
    "AgentProposalStatus": {
      "description": "One AI candidate held for human review in a manual-mode run. Tag-broken\ncandidates never become proposals (they are recorded as failures), so a\npending proposal is always applicable.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "pending",
            "applied",
            "rejected"
          ]
        },
        {
          "description": "The live segment moved (edited, locked, or confirmed by a human)\nbefore the proposal was applied; the human state wins.",
          "type": "string",
          "const": "stale"
        }
      ]
    },
    "AgentReviewDecision": {
      "type": "string",
      "enum": [
        "apply",
        "reject"
      ]
    },
    "AgentReviewParams": {
      "description": "Human decision on pending manual-mode proposals, one or many at a time.\n`apply` writes each draft through the same guards as auto mode (live row\nstill untranslated and unlocked) and refreshes that segment's QA in the\nsame transaction; a moved row turns the proposal `stale` instead of\noverwriting human work. `reject` records the decision and writes nothing.",
      "type": "object",
      "properties": {
        "decision": {
          "$ref": "#/$defs/AgentReviewDecision"
        },
        "runId": {
          "type": "string"
        },
        "segmentIds": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "runId",
        "segmentIds",
        "decision"
      ]
    },
    "AgentRunStatus": {
      "description": "Lifecycle of an agent run. The run never confirms segments, never signs\noff, and never exports: it always parks at `awaitingReview` for a human.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "running",
            "canceled",
            "failed"
          ]
        },
        {
          "description": "Terminal human gate: drafts are in the grid, a person decides what\ngets confirmed or exported.",
          "type": "string",
          "const": "awaitingReview"
        }
      ]
    },
    "AgentRunView": {
      "description": "The observable task order for one agent run.",
      "type": "object",
      "properties": {
        "aiDrafted": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "approvalMode": {
          "$ref": "#/$defs/AgentApprovalMode"
        },
        "autoConfirmed": {
          "description": "Turbo mode: segments confirmed through `segment.confirm` after the\nsegment-scoped QA gate found zero open error-severity issues.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "cancelRequested": {
          "type": "boolean"
        },
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "documentId": {
          "type": "string"
        },
        "eligibleSegments": {
          "description": "Untranslated, unlocked segments in the requested scope before the\ncap — makes any truncation explicit.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "failedSegmentIds": {
          "description": "Segment ids behind `failedSegments`, for the precise rerun.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "failedSegments": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "model": {
          "type": "string"
        },
        "openQaIssues": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "plannedSegments": {
          "description": "Untranslated segments claimed by this run at start time (after the\n`maxSegments` cap).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "processedSegments": {
          "description": "Planned segments that reached a per-segment outcome (TM applied, AI\ndraft applied, proposal queued, failed, or skipped). The honest\nprogress numerator; `plannedSegments` is the denominator.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "profileId": {
          "description": "The resolved profile drafting this run.",
          "type": "string"
        },
        "proposals": {
          "description": "Manual mode: the review queue. Empty in auto and turbo runs.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AgentProposal"
          }
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        },
        "runId": {
          "type": "string"
        },
        "skippedSegments": {
          "description": "Planned segments left untouched because a human edited or locked\nthem while the run was in flight.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "status": {
          "$ref": "#/$defs/AgentRunStatus"
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/AgentStep"
          }
        },
        "tmApplied": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "runId",
        "documentId",
        "status",
        "approvalMode",
        "profileId",
        "provider",
        "model",
        "cancelRequested",
        "plannedSegments",
        "eligibleSegments",
        "processedSegments",
        "tmApplied",
        "aiDrafted",
        "skippedSegments",
        "failedSegments",
        "failedSegmentIds",
        "autoConfirmed",
        "openQaIssues",
        "proposals",
        "steps",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "AgentStartParams": {
      "type": "object",
      "properties": {
        "approvalMode": {
          "description": "Approval tier; `manual` when omitted.",
          "$ref": "#/$defs/AgentApprovalMode",
          "default": "manual"
        },
        "documentId": {
          "type": "string"
        },
        "instruction": {
          "type": [
            "string",
            "null"
          ]
        },
        "maxSegments": {
          "description": "Upper bound on segments the agent may touch in one run.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "profileId": {
          "description": "Profile to draft with; the default profile when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "segmentIds": {
          "description": "Optional scope: only these segments are considered (intersected with\nthe document's untranslated, unlocked set). Powers \"run the current\nfilter\" and the precise failed-segment rerun.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "documentId"
      ]
    },
    "AgentStatusParams": {
      "type": "object",
      "properties": {
        "runId": {
          "type": "string"
        }
      },
      "required": [
        "runId"
      ]
    },
    "AgentStep": {
      "type": "object",
      "properties": {
        "detail": {
          "type": "string"
        },
        "index": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "kind": {
          "$ref": "#/$defs/AgentStepKind"
        },
        "segmentId": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/AgentStepStatus"
        }
      },
      "required": [
        "index",
        "kind",
        "status",
        "detail"
      ]
    },
    "AgentStepKind": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "plan",
            "qa",
            "summary",
            "cancel"
          ]
        },
        {
          "description": "Exact TM reuse during pretranslation.",
          "type": "string",
          "const": "tm"
        },
        {
          "description": "AI drafting for a TM miss.",
          "type": "string",
          "const": "translate"
        },
        {
          "description": "Manual mode: an AI candidate queued, approved, rejected, or went\nstale (`ai.agent.review` outcomes included).",
          "type": "string",
          "const": "proposal"
        },
        {
          "description": "Turbo mode: the auto-confirm decision for one drafted segment.",
          "type": "string",
          "const": "confirm"
        }
      ]
    },
    "AgentStepNotification": {
      "description": "Payload for the reserved `notify.ai.agent.step` frame emitted while a run\nis in flight. `runStatus` lets clients notice the terminal transition\nwithout polling.",
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "runId": {
          "type": "string"
        },
        "runStatus": {
          "$ref": "#/$defs/AgentRunStatus"
        },
        "step": {
          "$ref": "#/$defs/AgentStep"
        }
      },
      "required": [
        "runId",
        "documentId",
        "runStatus",
        "step"
      ]
    },
    "AgentStepStatus": {
      "type": "string",
      "enum": [
        "done",
        "failed",
        "skipped"
      ]
    },
    "AiAssistAction": {
      "oneOf": [
        {
          "description": "Draft a translation for the segment source.",
          "type": "string",
          "const": "translate"
        },
        {
          "description": "Improve the segment's current target.",
          "type": "string",
          "const": "refine"
        }
      ]
    },
    "AiAssistCancelParams": {
      "type": "object",
      "properties": {
        "assistId": {
          "type": "string"
        }
      },
      "required": [
        "assistId"
      ]
    },
    "AiAssistParams": {
      "type": "object",
      "properties": {
        "action": {
          "$ref": "#/$defs/AiAssistAction"
        },
        "instruction": {
          "type": [
            "string",
            "null"
          ]
        },
        "profileId": {
          "description": "Profile to call; the default profile when omitted. Requests for the\nsame segment through *different* profiles run in parallel (the\nmulti-candidate path); a second request on the same (segment,\nprofile) pair is a Conflict.",
          "type": [
            "string",
            "null"
          ]
        },
        "segmentId": {
          "type": "string"
        }
      },
      "required": [
        "segmentId",
        "action"
      ]
    },
    "AiAssistResult": {
      "type": "object",
      "properties": {
        "draftTarget": {
          "type": "string"
        },
        "elapsedMs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "model": {
          "type": "string"
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        },
        "tagCheck": {
          "description": "Placeholder/tag integrity of the draft against the segment source.",
          "$ref": "#/$defs/TagIntegrityReport"
        }
      },
      "required": [
        "draftTarget",
        "provider",
        "model",
        "elapsedMs",
        "tagCheck"
      ]
    },
    "AiAssistRunStatus": {
      "description": "Lifecycle of one asynchronous assist request. `ai.assist.start` validates\nand returns immediately; the provider call runs off the RPC thread and the\nclient polls `ai.assist.status` until the run turns terminal. Assist never\nwrites to the segment: a `done` run only carries a proposal for a human.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "running"
          ]
        },
        {
          "description": "The provider answered; `result` carries the proposal and tag verdict.",
          "type": "string",
          "const": "done"
        },
        {
          "description": "The provider call failed; `errorMessage` says why. Never fabricated.",
          "type": "string",
          "const": "failed"
        },
        {
          "description": "Cancellation was requested and honored; any late result is discarded.",
          "type": "string",
          "const": "canceled"
        }
      ]
    },
    "AiAssistRunView": {
      "description": "The observable state of one assist request.",
      "type": "object",
      "properties": {
        "action": {
          "$ref": "#/$defs/AiAssistAction"
        },
        "assistId": {
          "type": "string"
        },
        "cancelRequested": {
          "type": "boolean"
        },
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "errorMessage": {
          "description": "Present exactly when `status` is `failed`.",
          "type": [
            "string",
            "null"
          ]
        },
        "profileId": {
          "description": "The resolved profile serving this request.",
          "type": "string"
        },
        "result": {
          "description": "Present exactly when `status` is `done`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AiAssistResult"
            },
            {
              "type": "null"
            }
          ]
        },
        "segmentId": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/AiAssistRunStatus"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "assistId",
        "segmentId",
        "profileId",
        "action",
        "status",
        "cancelRequested",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "AiAssistStatusParams": {
      "type": "object",
      "properties": {
        "assistId": {
          "type": "string"
        }
      },
      "required": [
        "assistId"
      ]
    },
    "AiConfigureParams": {
      "type": "object",
      "properties": {
        "apiKey": {
          "description": "Held in engine memory only; never persisted to disk.",
          "type": "string"
        },
        "baseUrl": {
          "description": "Overrides the provider's default base URL. Required for\n`openaiCompatible`, optional otherwise.",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "type": "string"
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        }
      },
      "required": [
        "provider",
        "model",
        "apiKey"
      ]
    },
    "AiProfileAddParams": {
      "description": "Add one provider profile to the engine's in-memory list. Credentials\nfollow the `ai.configure` rules exactly: engine memory only, never\npersisted, never echoed back.",
      "type": "object",
      "properties": {
        "apiKey": {
          "description": "Held in engine memory only; never persisted to disk.",
          "type": "string"
        },
        "baseUrl": {
          "description": "Overrides the provider's default base URL. Required for\n`openaiCompatible`, optional otherwise.",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Display label; defaults to \"provider · model\" when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "type": "string"
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        }
      },
      "required": [
        "provider",
        "model",
        "apiKey"
      ]
    },
    "AiProfileListParams": {
      "type": "object"
    },
    "AiProfileListResult": {
      "type": "object",
      "properties": {
        "defaultProfileId": {
          "type": [
            "string",
            "null"
          ]
        },
        "profiles": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/AiProfileView"
          }
        }
      },
      "required": [
        "profiles"
      ]
    },
    "AiProfileRemoveParams": {
      "type": "object",
      "properties": {
        "profileId": {
          "type": "string"
        }
      },
      "required": [
        "profileId"
      ]
    },
    "AiProfileView": {
      "description": "The observable face of one configured profile. The credential is never\npart of this view.",
      "type": "object",
      "properties": {
        "baseUrl": {
          "type": "string"
        },
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "label": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "profileId": {
          "type": "string"
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        }
      },
      "required": [
        "profileId",
        "provider",
        "model",
        "baseUrl",
        "label",
        "createdAtMs"
      ]
    },
    "AiProviderKind": {
      "type": "string",
      "enum": [
        "openai",
        "openaiResponses",
        "anthropic",
        "gemini",
        "deepl",
        "deepseek",
        "qwen",
        "glm",
        "kimi",
        "volcengine",
        "openaiCompatible"
      ]
    },
    "AiStatusParams": {
      "type": "object"
    },
    "AiStatusResult": {
      "description": "`provider`/`model` describe the default profile; `profileCount` counts\nevery configured profile (`ai.profile.list` has the full views).",
      "type": "object",
      "properties": {
        "configured": {
          "type": "boolean"
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "profileCount": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "provider": {
          "anyOf": [
            {
              "$ref": "#/$defs/AiProviderKind"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "configured"
      ]
    },
    "DegradationFinding": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "severity": {
          "$ref": "#/$defs/DegradationSeverity"
        },
        "structuralPath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "code",
        "severity",
        "message"
      ]
    },
    "DegradationSeverity": {
      "type": "string",
      "enum": [
        "warning",
        "error"
      ]
    },
    "Document": {
      "type": "object",
      "properties": {
        "currentVersion": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "degradation": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DegradationFinding"
          }
        },
        "filterId": {
          "type": "string"
        },
        "format": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "importedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "name": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        },
        "relativePath": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "segmentCount": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "sourceSha256": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/DocumentStatus"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "projectId",
        "name",
        "relativePath",
        "format",
        "filterId",
        "sourceSha256",
        "currentVersion",
        "status",
        "revision",
        "segmentCount",
        "degradation",
        "importedAtMs",
        "updatedAtMs"
      ]
    },
    "DocumentExportParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "outputPath": {
          "type": "string"
        },
        "overrideQaGate": {
          "description": "Pass the QA export gate this once. When the project's QA profile has\n`blockExportOnError`, the engine re-checks the document before\nexporting and refuses with `exportBlocked` (error `data.reason` =\n`\"qaGate\"`, plus open-error count and leading rule ids) while\nerror-severity open issues exist. Same honest pattern as `overwrite`:\nrefuse → the user decides explicitly → pass. Defaults to false.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "overwrite": {
          "description": "Replace an existing destination file (staged sibling temp + atomic\nrename). Defaults to false: the export is refused with `exportBlocked`\nwhen the path exists. Even with overwrite, the engine refuses paths\ninside its own managed data directory — it cannot tell who owns an\narbitrary file on disk, but its own project data it can protect.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "segmentAnchors": {
          "description": "Embed per-paragraph grid-segment anchors into the exported artifact so\na layout preview can map clicks back to segments. Preview aid; filters\nwithout anchor support ignore it. Defaults to a plain export.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "documentId",
        "outputPath"
      ]
    },
    "DocumentExportResult": {
      "type": "object",
      "properties": {
        "degradation": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DegradationFinding"
          }
        },
        "outputPath": {
          "type": "string"
        },
        "translatedSegments": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "outputPath",
        "translatedSegments",
        "degradation"
      ]
    },
    "DocumentImportParams": {
      "type": "object",
      "properties": {
        "filterId": {
          "description": "Explicit filter id. When omitted, the engine probes registered filters.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectId": {
          "type": "string"
        },
        "segmentation": {
          "description": "Segmentation mode: `sentence` or `paragraph`. When omitted together\nwith `srxPath`, the project's stored default applies (falling back\nto sentence with built-in rules).",
          "type": [
            "string",
            "null"
          ]
        },
        "sourcePath": {
          "type": "string"
        },
        "srxPath": {
          "description": "Path to a custom SRX ruleset used for sentence segmentation. When\nomitted together with `segmentation`, the project's stored default\napplies; when provided without `segmentation` it implies sentence\nmode. An explicit `segmentation` makes the params the complete\nchoice, so `srxPath: null` then means the built-in rules.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "projectId",
        "sourcePath"
      ]
    },
    "DocumentImportResult": {
      "type": "object",
      "properties": {
        "document": {
          "$ref": "#/$defs/Document"
        },
        "segmentCount": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "document",
        "segmentCount"
      ]
    },
    "DocumentListParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId"
      ]
    },
    "DocumentListResult": {
      "type": "object",
      "properties": {
        "documents": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Document"
          }
        },
        "progress": {
          "description": "Per-document segment-state and open-QA counts, aligned with\n`documents` (same order), so the file rail can show honest progress\nwithout materializing any segment rows client-side.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DocumentProgress"
          }
        }
      },
      "required": [
        "documents",
        "progress"
      ]
    },
    "DocumentProgress": {
      "description": "Segment progress of one document, counted in SQL at list time.",
      "type": "object",
      "properties": {
        "counts": {
          "$ref": "#/$defs/SegmentCounts"
        },
        "documentId": {
          "type": "string"
        }
      },
      "required": [
        "documentId",
        "counts"
      ]
    },
    "DocumentRemoveParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        }
      },
      "required": [
        "documentId"
      ]
    },
    "DocumentRemoveResult": {
      "description": "What `document.remove` deleted and what it deliberately kept. The\ndocument row, its segments, and its QA issues are gone (one SQLite\ntransaction); the project TM — including entries confirmed from this\ndocument — and termbases are untouched by design.",
      "type": "object",
      "properties": {
        "document": {
          "description": "The removed document's last metadata, echoed for the status line.",
          "$ref": "#/$defs/Document"
        },
        "managedCopyDeleted": {
          "description": "Whether the engine deleted its own managed copy of the imported\nsource file (the copy under the engine data directory). The original\nfile at the import path is never touched, and a managed path that\nresolves outside the data directory (possible for legacy imports) is\nleft alone too — the engine cannot tell who owns it.",
          "type": "boolean"
        },
        "removedQaIssues": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "removedSegments": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "document",
        "removedSegments",
        "removedQaIssues",
        "managedCopyDeleted"
      ]
    },
    "DocumentStatus": {
      "type": "string",
      "enum": [
        "active",
        "failed",
        "superseded"
      ]
    },
    "EditorPreferences": {
      "type": "object",
      "properties": {
        "autocomplete": {
          "type": "boolean"
        },
        "cjkSpacing": {
          "type": "boolean"
        },
        "punctuationAssistance": {
          "type": "boolean"
        },
        "shortcuts": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "showNonprinting": {
          "type": "boolean"
        },
        "theme": {
          "type": "string"
        },
        "zoom": {
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        }
      },
      "required": [
        "theme",
        "zoom",
        "showNonprinting",
        "autocomplete",
        "cjkSpacing",
        "punctuationAssistance",
        "shortcuts"
      ]
    },
    "EngineCapabilities": {
      "type": "object",
      "properties": {
        "aiAgent": {
          "type": "boolean"
        },
        "aiAssist": {
          "type": "boolean"
        },
        "filters": {
          "description": "Registered document filter ids, e.g. `builtin.docx`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "notifications": {
          "description": "Whether the engine emits notification frames.",
          "type": "boolean"
        }
      },
      "required": [
        "filters",
        "aiAssist",
        "aiAgent",
        "notifications"
      ]
    },
    "EngineReadyNotification": {
      "description": "Payload for the reserved `notify.engine.ready` frame emitted on startup.",
      "type": "object",
      "properties": {
        "engineName": {
          "type": "string"
        },
        "engineVersion": {
          "type": "string"
        },
        "protocolVersion": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "engineName",
        "engineVersion",
        "protocolVersion"
      ]
    },
    "HarnessCancelParams": {
      "type": "object",
      "properties": {
        "harnessId": {
          "type": "string"
        }
      },
      "required": [
        "harnessId"
      ]
    },
    "HarnessRunStatus": {
      "description": "Lifecycle of one harness run. Like the batch agent, the run never\nconfirms and never exports: every terminal state leaves drafts (if any)\nparked at the human review gate.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "running",
            "failed",
            "canceled"
          ]
        },
        {
          "description": "Terminal: the model finished (or the turn budget ran out); drafts\nare in the grid awaiting a human.",
          "type": "string",
          "const": "awaitingReview"
        }
      ]
    },
    "HarnessRunView": {
      "description": "The observable state of one harness run.",
      "type": "object",
      "properties": {
        "cancelRequested": {
          "type": "boolean"
        },
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "documentId": {
          "type": "string"
        },
        "draftedSegments": {
          "description": "Drafts written through `write_draft` (guards passed).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "errorMessage": {
          "description": "Present exactly when `status` is `failed`.",
          "type": [
            "string",
            "null"
          ]
        },
        "harnessId": {
          "type": "string"
        },
        "instruction": {
          "type": "string"
        },
        "maxTurns": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "model": {
          "type": "string"
        },
        "notes": {
          "description": "The model's own scratchpad, in order. Never pruned by the budget.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "profileId": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        },
        "provider": {
          "$ref": "#/$defs/AiProviderKind"
        },
        "status": {
          "$ref": "#/$defs/HarnessRunStatus"
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/HarnessStep"
          }
        },
        "summary": {
          "description": "Present when terminal via `finish` (or the turn budget note).",
          "type": [
            "string",
            "null"
          ]
        },
        "termsAdded": {
          "description": "Term entries written through `term_add`.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "turnsUsed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "webAccess": {
          "type": "boolean"
        }
      },
      "required": [
        "harnessId",
        "projectId",
        "documentId",
        "status",
        "instruction",
        "profileId",
        "provider",
        "model",
        "webAccess",
        "maxTurns",
        "turnsUsed",
        "cancelRequested",
        "draftedSegments",
        "termsAdded",
        "notes",
        "steps",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "HarnessStartParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "instruction": {
          "description": "The task brief. Optional: the default brief is \"translate this\ndocument\" with the project's language pair.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxTurns": {
          "description": "Model/tool turns before the honest circuit breaker trips.\nDefault 24, cap 64.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "profileId": {
          "description": "Profile to drive the conversation; the default profile when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "webAccess": {
          "description": "Enables the `web_fetch` tool for this run. Off by default: network\nreach is an explicit human decision, never an ambient capability.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "documentId"
      ]
    },
    "HarnessStatusParams": {
      "type": "object",
      "properties": {
        "harnessId": {
          "type": "string"
        }
      },
      "required": [
        "harnessId"
      ]
    },
    "HarnessStep": {
      "type": "object",
      "properties": {
        "detail": {
          "type": "string"
        },
        "index": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "kind": {
          "$ref": "#/$defs/HarnessStepKind"
        },
        "segmentId": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/AgentStepStatus"
        }
      },
      "required": [
        "index",
        "kind",
        "status",
        "detail"
      ]
    },
    "HarnessStepKind": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "summary",
            "cancel"
          ]
        },
        {
          "description": "One model turn (the reply's tool call, or the parse failure).",
          "type": "string",
          "const": "model"
        },
        {
          "description": "A tool executed on the engine thread (read/lookup/qa/term).",
          "type": "string",
          "const": "tool"
        },
        {
          "description": "`write_draft` landed (or was refused by a guard).",
          "type": "string",
          "const": "draft"
        },
        {
          "description": "The model appended to its run notes.",
          "type": "string",
          "const": "note"
        },
        {
          "description": "A `qa_run` tool call.",
          "type": "string",
          "const": "qa"
        },
        {
          "description": "A `web_fetch` performed by the worker.",
          "type": "string",
          "const": "web"
        }
      ]
    },
    "HarnessStepNotification": {
      "description": "Payload for the reserved `notify.ai.harness.step` frame.",
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "harnessId": {
          "type": "string"
        },
        "runStatus": {
          "$ref": "#/$defs/HarnessRunStatus"
        },
        "step": {
          "$ref": "#/$defs/HarnessStep"
        }
      },
      "required": [
        "harnessId",
        "documentId",
        "runStatus",
        "step"
      ]
    },
    "InitializeParams": {
      "type": "object",
      "properties": {
        "clientName": {
          "type": "string"
        },
        "clientVersion": {
          "type": "string"
        },
        "protocolVersion": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "protocolVersion",
        "clientName",
        "clientVersion"
      ]
    },
    "InitializeResult": {
      "type": "object",
      "properties": {
        "capabilities": {
          "$ref": "#/$defs/EngineCapabilities"
        },
        "engineName": {
          "type": "string"
        },
        "engineVersion": {
          "type": "string"
        },
        "protocolVersion": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "protocolVersion",
        "engineName",
        "engineVersion",
        "capabilities"
      ]
    },
    "Memory": {
      "description": "One translation memory: a named store of confirmed segment pairs.\n`tm_entries.memory_id` points here. Projects reach a memory through a\n[`MemoryMount`]; the memory itself carries no project binding.",
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sourceLocale": {
          "type": "string"
        },
        "targetLocale": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "name",
        "sourceLocale",
        "targetLocale",
        "revision",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "MemoryAttachParams": {
      "type": "object",
      "properties": {
        "memoryId": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "memoryId"
      ]
    },
    "MemoryAttachResult": {
      "description": "New mounts are enabled for lookup but never writable: promoting the\nworking memory is always an explicit `memory.update`, so a confirm can\nnever silently start writing into a freshly attached memory.",
      "type": "object",
      "properties": {
        "mount": {
          "$ref": "#/$defs/MemoryMount"
        }
      },
      "required": [
        "mount"
      ]
    },
    "MemoryCreateParams": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "sourceLocale": {
          "type": "string"
        },
        "targetLocale": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "sourceLocale",
        "targetLocale"
      ]
    },
    "MemoryDeleteParams": {
      "description": "`memory.delete` — remove a memory row for good. Honest conflicts, never\na silent orphan: mounted anywhere fails (detach it from every project\nfirst), and entries remaining fails unless `deleteEntries` explicitly\nasks for the cascade. The conflict message names the entry count so the\ncaller can put a real number in front of the user.",
      "type": "object",
      "properties": {
        "deleteEntries": {
          "description": "Delete the memory's TM entries along with it. Defaults to false:\na memory that still holds entries conflicts instead.",
          "type": "boolean",
          "default": false
        },
        "memoryId": {
          "type": "string"
        }
      },
      "required": [
        "memoryId"
      ]
    },
    "MemoryDeleteResult": {
      "type": "object",
      "properties": {
        "deletedEntries": {
          "description": "TM entries removed in the same transaction (0 for an empty memory).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "memory": {
          "description": "The memory as it was at deletion time.",
          "$ref": "#/$defs/Memory"
        }
      },
      "required": [
        "memory",
        "deletedEntries"
      ]
    },
    "MemoryDetachParams": {
      "type": "object",
      "properties": {
        "memoryId": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "memoryId"
      ]
    },
    "MemoryDetachResult": {
      "description": "Carries the removed mount. Detaching a memory that is not mounted fails\nwith `notFound` instead of pretending success. Detaching the writable\nmount is allowed and leaves the project without a working memory —\nconfirms then fail honestly until another mount is promoted.",
      "type": "object",
      "properties": {
        "mount": {
          "$ref": "#/$defs/MemoryMount"
        }
      },
      "required": [
        "mount"
      ]
    },
    "MemoryListParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "description": "When set, `mounts` is restricted to this project.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "MemoryListResult": {
      "type": "object",
      "properties": {
        "memories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Memory"
          }
        },
        "mounts": {
          "description": "Mounts in (project, priority) order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/MemoryMount"
          }
        }
      },
      "required": [
        "memories",
        "mounts"
      ]
    },
    "MemoryMount": {
      "description": "A project's mount of one memory — the same family shape as\n[`TermbaseMount`]. `enabled` gates the read path (lookup, pretranslate);\n`writable` marks the working memory, the single mount confirmation-time\nTM writes go to. The engine enforces at most one writable mount per\nproject.",
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "enabled": {
          "type": "boolean"
        },
        "memoryId": {
          "type": "string"
        },
        "priority": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "writable": {
          "type": "boolean"
        }
      },
      "required": [
        "projectId",
        "memoryId",
        "priority",
        "enabled",
        "writable",
        "revision",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "MemoryRenameParams": {
      "description": "`memory.rename` — rename the memory itself (mount edits stay in\n`memory.update`). The new name applies everywhere the memory is\nmounted; entries and mounts are untouched.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Optimistic concurrency: must match the memory's current revision.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "memoryId": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "memoryId",
        "name",
        "baseRevision"
      ]
    },
    "MemoryRenameResult": {
      "type": "object",
      "properties": {
        "memory": {
          "$ref": "#/$defs/Memory"
        }
      },
      "required": [
        "memory"
      ]
    },
    "MemoryUpdateParams": {
      "description": "Edit one mount: enable/disable the read path, promote/demote the\nworking memory, and/or move the mount to a new priority position.\nOmitted fields stay unchanged.\n\n`writable: true` while another mount is writable fails with `conflict`\n(demote the current working memory first) — the engine never lets two\nmounts receive confirmation-time writes.",
      "type": "object",
      "properties": {
        "enabled": {
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "memoryId": {
          "type": "string"
        },
        "priority": {
          "description": "Target position in the project's mount list (0 = highest priority).\nValues past the end clamp to the last position. Sibling mounts are\nrenumbered to keep priorities contiguous.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "writable": {
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "projectId",
        "memoryId"
      ]
    },
    "MemoryUpdateResult": {
      "description": "The project's mounts after the edit, in priority order — a priority\nmove renumbers siblings, so one mount alone would hide real changes.",
      "type": "object",
      "properties": {
        "mounts": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/MemoryMount"
          }
        }
      },
      "required": [
        "mounts"
      ]
    },
    "MethodContract": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/InitializeParams"
        },
        "result": {
          "$ref": "#/$defs/InitializeResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract10": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/DocumentRemoveParams"
        },
        "result": {
          "$ref": "#/$defs/DocumentRemoveResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract11": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/DocumentExportParams"
        },
        "result": {
          "$ref": "#/$defs/DocumentExportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract12": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentListParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract13": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentUpdateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract14": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentUpdateSourceParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentUpdateSourceResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract15": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentReplaceParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentReplaceResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract16": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentConfirmParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentConfirmResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract17": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/SegmentLockParams"
        },
        "result": {
          "$ref": "#/$defs/SegmentLockResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract18": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmLookupParams"
        },
        "result": {
          "$ref": "#/$defs/TmLookupResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract19": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmListParams"
        },
        "result": {
          "$ref": "#/$defs/TmListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract2": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectListParams"
        },
        "result": {
          "$ref": "#/$defs/ShutdownResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract20": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/TmUpdateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract21": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmDeleteParams"
        },
        "result": {
          "$ref": "#/$defs/TmDeleteResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract22": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmImportParams"
        },
        "result": {
          "$ref": "#/$defs/TmImportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract23": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmExportParams"
        },
        "result": {
          "$ref": "#/$defs/TmExportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract24": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TmPretranslateParams"
        },
        "result": {
          "$ref": "#/$defs/TmPretranslateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract25": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryCreateParams"
        },
        "result": {
          "$ref": "#/$defs/Memory"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract26": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryListParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract27": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryAttachParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryAttachResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract28": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryDetachParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryDetachResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract29": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryUpdateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract3": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectCreateParams"
        },
        "result": {
          "$ref": "#/$defs/Project"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract30": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryRenameParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryRenameResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract31": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/MemoryDeleteParams"
        },
        "result": {
          "$ref": "#/$defs/MemoryDeleteResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract32": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseCreateParams"
        },
        "result": {
          "$ref": "#/$defs/Termbase"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract33": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseListParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract34": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseAttachParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseAttachResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract35": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseDetachParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseDetachResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract36": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseUpdateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract37": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseImportParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseImportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract38": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermbaseExportParams"
        },
        "result": {
          "$ref": "#/$defs/TermbaseExportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract39": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermAddParams"
        },
        "result": {
          "$ref": "#/$defs/TermAddResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract4": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectListParams"
        },
        "result": {
          "$ref": "#/$defs/ProjectListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract40": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/TermUpdateResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract41": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermDeleteParams"
        },
        "result": {
          "$ref": "#/$defs/TermDeleteResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract42": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermListParams"
        },
        "result": {
          "$ref": "#/$defs/TermListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract43": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/TermLookupParams"
        },
        "result": {
          "$ref": "#/$defs/TermLookupResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract44": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaRunParams"
        },
        "result": {
          "$ref": "#/$defs/QaRunResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract45": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaListParams"
        },
        "result": {
          "$ref": "#/$defs/QaListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract46": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaWaiveParams"
        },
        "result": {
          "$ref": "#/$defs/QaWaiveResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract47": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaFixListParams"
        },
        "result": {
          "$ref": "#/$defs/QaFixListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract48": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaFixApplyParams"
        },
        "result": {
          "$ref": "#/$defs/QaFixApplyResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract49": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaProfileGetParams"
        },
        "result": {
          "$ref": "#/$defs/QaProfileView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract5": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectGetParams"
        },
        "result": {
          "$ref": "#/$defs/Project"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract50": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/QaProfileUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/QaProfileView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract51": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiConfigureParams"
        },
        "result": {
          "$ref": "#/$defs/AiStatusResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract52": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiStatusParams"
        },
        "result": {
          "$ref": "#/$defs/AiStatusResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract53": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiProfileAddParams"
        },
        "result": {
          "$ref": "#/$defs/AiProfileListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract54": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiProfileListParams"
        },
        "result": {
          "$ref": "#/$defs/AiProfileListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract55": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiProfileRemoveParams"
        },
        "result": {
          "$ref": "#/$defs/AiProfileListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract56": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiAssistParams"
        },
        "result": {
          "$ref": "#/$defs/AiAssistRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract57": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiAssistStatusParams"
        },
        "result": {
          "$ref": "#/$defs/AiAssistRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract58": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AiAssistCancelParams"
        },
        "result": {
          "$ref": "#/$defs/AiAssistRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract59": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AgentStartParams"
        },
        "result": {
          "$ref": "#/$defs/AgentRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract6": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectUpdateParams"
        },
        "result": {
          "$ref": "#/$defs/Project"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract60": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AgentStatusParams"
        },
        "result": {
          "$ref": "#/$defs/AgentRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract61": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AgentReviewParams"
        },
        "result": {
          "$ref": "#/$defs/AgentRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract62": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/AgentCancelParams"
        },
        "result": {
          "$ref": "#/$defs/AgentRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract63": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/HarnessStartParams"
        },
        "result": {
          "$ref": "#/$defs/HarnessRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract64": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/HarnessStatusParams"
        },
        "result": {
          "$ref": "#/$defs/HarnessRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract65": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/HarnessCancelParams"
        },
        "result": {
          "$ref": "#/$defs/HarnessRunView"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract7": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/ProjectArchiveParams"
        },
        "result": {
          "$ref": "#/$defs/Project"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract8": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/DocumentImportParams"
        },
        "result": {
          "$ref": "#/$defs/DocumentImportResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "MethodContract9": {
      "description": "A `{ params, result }` pair for one method. Only used for schema export.",
      "type": "object",
      "properties": {
        "params": {
          "$ref": "#/$defs/DocumentListParams"
        },
        "result": {
          "$ref": "#/$defs/DocumentListResult"
        }
      },
      "additionalProperties": false,
      "required": [
        "params",
        "result"
      ]
    },
    "NotificationCatalog": {
      "description": "Notification-name-keyed catalog for the reserved notification frames.",
      "type": "object",
      "properties": {
        "notify.ai.agent.step": {
          "$ref": "#/$defs/AgentStepNotification"
        },
        "notify.ai.harness.step": {
          "$ref": "#/$defs/HarnessStepNotification"
        },
        "notify.engine.ready": {
          "$ref": "#/$defs/EngineReadyNotification"
        }
      },
      "additionalProperties": false,
      "required": [
        "notify.engine.ready",
        "notify.ai.agent.step",
        "notify.ai.harness.step"
      ]
    },
    "NumberEvidence": {
      "description": "Evidence attached to a QA issue. Historically number-only; general rules\nreuse the same shape with free-form source/target values.",
      "type": "object",
      "properties": {
        "relatedSegmentIds": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "sourceNumbers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "sourceValues": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "targetNumbers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "targetValues": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "sourceNumbers",
        "targetNumbers"
      ]
    },
    "Project": {
      "type": "object",
      "properties": {
        "archivedAtMs": {
          "type": [
            "integer",
            "null"
          ],
          "format": "int64",
          "default": null
        },
        "configuration": {
          "$ref": "#/$defs/ProjectConfiguration"
        },
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "domain": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "lifecycle": {
          "$ref": "#/$defs/ProjectLifecycle"
        },
        "name": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sourceLocale": {
          "type": "string"
        },
        "targetLocale": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "name",
        "sourceLocale",
        "targetLocale",
        "domain",
        "lifecycle",
        "revision",
        "configuration",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "ProjectArchiveParams": {
      "description": "Parameters for `project.archive`. `archived: true` (the default) moves the\nlifecycle to `archived` and stamps `archivedAtMs`; `archived: false`\nrestores `active` and clears the stamp. Both directions are idempotent.",
      "type": "object",
      "properties": {
        "archived": {
          "type": "boolean",
          "default": true
        },
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId"
      ]
    },
    "ProjectConfiguration": {
      "type": "object",
      "properties": {
        "aiProfileIds": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "analysisProfileId": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "editorDefaults": {
          "anyOf": [
            {
              "$ref": "#/$defs/EditorPreferences"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "engineAllowlist": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "pipelineId": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "qaProfile": {
          "description": "Project-level QA profile overrides (severity remaps, settings, export\ngate) applied over the built-in profile named by `qa_profile_id`.",
          "anyOf": [
            {
              "$ref": "#/$defs/QaProfileOverrides"
            },
            {
              "type": "null"
            }
          ]
        },
        "qaProfileId": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "segmentation": {
          "description": "Default segmentation for future imports. `None` means sentence mode.",
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectSegmentation"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "srxPath": {
          "description": "Default SRX ruleset path for future sentence-mode imports. Only the\npath is stored — a missing or invalid file fails at import time, not\nwhen the default is saved. Ignored (but kept) while the segmentation\ndefault is paragraph, so switching back to sentence restores it.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "taskPackage": {
          "anyOf": [
            {
              "$ref": "#/$defs/TaskPackageProjectReference"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "templateId": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "ProjectCreateParams": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "sourceLocale": {
          "type": "string"
        },
        "targetLocale": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "sourceLocale",
        "targetLocale"
      ]
    },
    "ProjectGetParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId"
      ]
    },
    "ProjectLifecycle": {
      "type": "string",
      "enum": [
        "active",
        "archived",
        "trash"
      ]
    },
    "ProjectListParams": {
      "type": "object"
    },
    "ProjectListResult": {
      "type": "object",
      "properties": {
        "projects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Project"
          }
        }
      },
      "required": [
        "projects"
      ]
    },
    "ProjectSegmentation": {
      "description": "Default segmentation mode applied when `document.import` is called without\nan explicit segmentation choice. Serialized as `sentence` / `paragraph`,\nmatching the strings `DocumentImportParams.segmentation` accepts.",
      "type": "string",
      "enum": [
        "sentence",
        "paragraph"
      ]
    },
    "ProjectUpdateParams": {
      "description": "Parameters for `project.update`. Omitted fields stay unchanged. `name`,\n`sourceLocale`, and `targetLocale` are trimmed and must not be empty when\nprovided.\n\nLanguage-pair rule: the source/target locales may only change while the\nproject holds no linguistic assets — no imported documents, no project-TM\nentries, and no attached termbases. Documents were segmented with the old\nsource locale and TM/term data was collected for the old pair, so once\nassets exist a locale change is rejected with `conflict` instead of\nsilently orphaning TM and term lookups.\n\nImport-default rule: `segmentation` (`sentence` | `paragraph`) and\n`srxPath` persist the project's default import choices in\n`configuration`. Empty or omitted values keep the current defaults;\n`clearSrxPath: true` resets the SRX default back to the built-in rules\n(and cannot be combined with a new `srxPath`). An `srxPath` is only\naccepted while the effective segmentation default is `sentence` — SRX\nrules never apply in paragraph mode. Only the path is stored: a missing\nSRX file fails honestly at import time, not when the default is saved.",
      "type": "object",
      "properties": {
        "clearSrxPath": {
          "description": "Reset the stored SRX default back to the built-in rules.",
          "type": "boolean",
          "default": false
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectId": {
          "type": "string"
        },
        "segmentation": {
          "description": "Default segmentation mode for future imports: `sentence` or\n`paragraph`. Empty or omitted keeps the current default.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "sourceLocale": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "srxPath": {
          "description": "Default SRX ruleset path for future sentence-mode imports. Empty or\nomitted keeps the current default; use `clearSrxPath` to reset.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "targetLocale": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "projectId"
      ]
    },
    "QaFix": {
      "description": "One engine-proposed correction. The client previews `fixedTargetText`\nverbatim and applies it through `qa.fix.apply` — it never invents or\nedits replacement text.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Segment revision the fix was computed against; pass it as\n`baseRevision` to `qa.fix.apply`.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "currentTargetText": {
          "description": "The target text the fix replaces (the segment's current text).",
          "type": "string"
        },
        "description": {
          "description": "Short English description of the mechanical change; clients\nlocalize by `ruleId`, like issue messages.",
          "type": "string"
        },
        "fixedTargetText": {
          "description": "The full replacement target text, applied verbatim.",
          "type": "string"
        },
        "issueId": {
          "type": "string"
        },
        "ruleId": {
          "type": "string"
        },
        "segmentId": {
          "type": "string"
        }
      },
      "required": [
        "issueId",
        "segmentId",
        "ruleId",
        "baseRevision",
        "currentTargetText",
        "fixedTargetText",
        "description"
      ]
    },
    "QaFixApplyParams": {
      "description": "`qa.fix.apply` — apply one correction through the exact `segment.update`\nguards: a stale `baseRevision` conflicts, a locked segment conflicts,\nand a confirmed segment honestly returns to draft. The engine recomputes\nthe correction from the current text (a finding without one conflicts);\nthe segment's QA refreshes in the same transaction. Applying never\nconfirms and never writes TM.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Optimistic concurrency: must match the segment's current revision.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "issueId": {
          "type": "string"
        }
      },
      "required": [
        "issueId",
        "baseRevision"
      ]
    },
    "QaFixApplyResult": {
      "type": "object",
      "properties": {
        "qaIssues": {
          "description": "The segment's full issue list after the same-transaction refresh.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/QaIssue"
          }
        },
        "segment": {
          "$ref": "#/$defs/Segment"
        }
      },
      "required": [
        "segment",
        "qaIssues"
      ]
    },
    "QaFixListParams": {
      "description": "`qa.fix.list` — the engine-proposed corrections for a document's open\nfindings (PRD S3 ④).\n\nCorrections are recomputed from each segment's *current* target text at\ncall time, never persisted: a stale issue whose text was already edited\nsimply stops producing one. Only mechanically fixable rules propose\nanything (edge whitespace, CJK half-width punctuation and the ASCII\nellipsis, adjacent repeated words, the unambiguous single-number\nmismatch); a finding without a correction is honestly absent from the\nlist. Locked segments are shielded — their findings never offer a fix.",
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        }
      },
      "required": [
        "documentId"
      ]
    },
    "QaFixListResult": {
      "type": "object",
      "properties": {
        "fixes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/QaFix"
          }
        }
      },
      "required": [
        "fixes"
      ]
    },
    "QaIssue": {
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "evidence": {
          "$ref": "#/$defs/NumberEvidence"
        },
        "fingerprint": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "params": {
          "description": "Structured message parameters (e.g. `{\"expected\": \"30\", \"found\":\n\"40\"}`) so clients can localize the finding; `message` stays the\nengine-produced English fallback. Empty for rules with nothing to\nparameterize; rows persisted before the field existed parse as empty.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "ruleId": {
          "type": "string"
        },
        "segmentId": {
          "type": "string"
        },
        "severity": {
          "$ref": "#/$defs/QaSeverity"
        },
        "status": {
          "$ref": "#/$defs/QaIssueStatus"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "waiveNote": {
          "description": "Free-form note recorded with a waiver. Optional by design — waiving\nmust not demand a ritual reason. Non-null only while `status` is\n[`QaIssueStatus::Waived`].",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "id",
        "segmentId",
        "ruleId",
        "severity",
        "status",
        "message",
        "fingerprint",
        "evidence",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "QaIssueStatus": {
      "description": "Lifecycle of a persisted QA issue.\n\n- `Open`: the finding reproduced on the latest run and nobody accepted it.\n- `Waived`: a user explicitly accepted this exact finding (`qa.waive`).\n  A waiver is pinned to the issue fingerprint, which hashes the rule,\n  segment, and evidence — so it holds only while the very same evidence\n  keeps reproducing. If the evidence changes, the changed finding opens\n  as a new issue instead of hiding behind the old waiver.\n- `Resolved`: the finding stopped reproducing (e.g. the numbers now\n  actually match). Only `qa.run` moves issues here; waiving never does.",
      "type": "string",
      "enum": [
        "open",
        "waived",
        "resolved"
      ]
    },
    "QaListParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "limit": {
          "description": "Page size. When omitted every issue from `offset` on is returned,\nwhich is the pre-paging behavior existing clients rely on.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "offset": {
          "description": "Issues to skip in list order (open first, then oldest); defaults to 0.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        }
      },
      "required": [
        "documentId"
      ]
    },
    "QaListResult": {
      "type": "object",
      "properties": {
        "issues": {
          "description": "One window of the document's issues: open first, then waived, then\nresolved.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/QaIssue"
          }
        },
        "total": {
          "description": "Issues for the document before the page window was applied, so\nclients can page honestly.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "issues",
        "total"
      ]
    },
    "QaProfileGetParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId"
      ]
    },
    "QaProfileOverrides": {
      "description": "Project-level QA profile overrides, applied over the resolved built-in\nprofile (memoQ convention: built-ins are immutable, the project layer is\na clone-then-override). Absent overrides mean the built-in profile runs\nexactly as shipped.",
      "type": "object",
      "properties": {
        "blockExportOnError": {
          "description": "Export gate: `document.export` refuses while error-severity open\nissues exist (an explicit `overrideQaGate` lets the user pass).\nOff by default — the gate is configured, never ambient.",
          "type": "boolean",
          "default": false
        },
        "settings": {
          "description": "Full replacement of the base profile's tunable settings. `None`\nkeeps the base profile's own values.",
          "anyOf": [
            {
              "$ref": "#/$defs/QaRuleSettings"
            },
            {
              "type": "null"
            }
          ]
        },
        "severityOverrides": {
          "description": "Per-rule severity remaps (rule id → severity) layered over the base\nprofile's table. Keys may name parameterized rules\n(`qa.term-missing:<id>`, `qa.regex:<id>`) as well as fixed ones.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/QaSeverity"
          }
        }
      }
    },
    "QaProfileUpdateParams": {
      "description": "`qa.profile.update` — write the project-level QA overrides. Omitted\nfields keep their stored values; provided fields replace them wholesale\n(`severityOverrides: {}` clears every remap). The engine compiles the\nmerged profile before storing anything, so a configuration that cannot\nrun is rejected instead of persisted.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Optimistic concurrency: must match the project's current revision.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "blockExportOnError": {
          "description": "Toggle the export gate.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "clearSettings": {
          "description": "Drop the stored settings replacement (mutually exclusive with\n`settings`).",
          "type": "boolean",
          "default": false
        },
        "projectId": {
          "type": "string"
        },
        "settings": {
          "description": "Replacement settings. `null` inside the option is not expressible —\nsend `clearSettings: true` to drop the project replacement and\nreturn to the base profile's values.",
          "anyOf": [
            {
              "$ref": "#/$defs/QaRuleSettings"
            },
            {
              "type": "null"
            }
          ]
        },
        "severityOverrides": {
          "description": "Replacement severity remap table. Keys must be rule ids\n(`qa.`-prefixed, including parameterized `qa.term-*:<id>` /\n`qa.regex:<id>` forms).",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "$ref": "#/$defs/QaSeverity"
          }
        }
      },
      "required": [
        "projectId",
        "baseRevision"
      ]
    },
    "QaProfileView": {
      "description": "The QA profile the engine will actually run for one project: the\nresolved built-in base plus the project-level overrides layered on it.\nBuilt-in profiles are immutable; the project layer is a clone-then-\noverride (memoQ convention), stored in the project configuration.\n\nReturned by both `qa.profile.get` and `qa.profile.update`.",
      "type": "object",
      "properties": {
        "baseProfileId": {
          "description": "The built-in profile the project resolves to (configured id when it\nnames a built-in, otherwise the target-locale default).",
          "type": "string"
        },
        "blockExportOnError": {
          "description": "Whether `document.export` refuses while error-severity open issues\nexist. Off by default.",
          "type": "boolean"
        },
        "enabledRuleIds": {
          "description": "The static rule ids the compiled profile runs, in lexicographic\norder — the rows a severity table can offer. Parameterized families\n(`qa.term-required:<id>`, `qa.term-forbidden:<id>`, `qa.regex:<id>`,\nand the `qa.tag` family marker behind the concrete `qa.tag-*` ids)\nexist per term/regex definition and are not listed.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "revision": {
          "description": "Project revision, for `qa.profile.update` optimistic concurrency.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "settings": {
          "description": "Effective settings: the project replacement when one is stored,\notherwise the base profile's own values.",
          "$ref": "#/$defs/QaRuleSettings"
        },
        "severityOverrides": {
          "description": "Project-level severity remaps (rule id → severity), layered over the\nbase profile's table. Built-in profiles ship without remaps, so this\nis also the effective table.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/QaSeverity"
          }
        }
      },
      "required": [
        "baseProfileId",
        "severityOverrides",
        "settings",
        "enabledRuleIds",
        "blockExportOnError",
        "revision"
      ]
    },
    "QaRuleSettings": {
      "description": "Tunable knobs of a QA profile (thresholds and locale-convention toggles).\nLives in the domain crate because project configuration stores a\nproject-level replacement of these values.",
      "type": "object",
      "properties": {
        "cjkPunctuation": {
          "type": "boolean"
        },
        "cjkSpacing": {
          "type": "boolean"
        },
        "maxLengthRatioPercent": {
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "maxTargetChars": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "minLengthRatioPercent": {
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "requireSentenceFinalPunctuation": {
          "type": "boolean"
        }
      },
      "required": [
        "minLengthRatioPercent",
        "maxLengthRatioPercent",
        "cjkSpacing",
        "cjkPunctuation",
        "requireSentenceFinalPunctuation"
      ]
    },
    "QaRunParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        }
      },
      "required": [
        "documentId"
      ]
    },
    "QaRunResult": {
      "type": "object",
      "properties": {
        "checkedSegments": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "issues": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/QaIssue"
          }
        },
        "openIssues": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "checkedSegments",
        "openIssues",
        "issues"
      ]
    },
    "QaSeverity": {
      "type": "string",
      "enum": [
        "error",
        "warning",
        "info"
      ]
    },
    "QaWaiveParams": {
      "description": "`qa.waive` — record a human decision about findings without pretending\nthey went away.\n\nWaiving never edits the segment, never confirms it, and never writes TM:\nthe numbers still disagree, and the issue rows say so. The waiver sticks\nexactly as long as later runs reproduce the same fingerprint (rule +\nsegment + evidence). When the evidence changes, the old row resolves and\nthe changed finding opens as a brand-new issue — a waiver never carries\nover to evidence the user has not seen.\n\nExactly one selector must be provided:\n\n- `issueId` — one issue. Waiving a resolved issue or restoring a\n  non-waived issue is a conflict.\n- `ruleId` + `documentId` — every issue of that rule in the document.\n- `segmentId` — every issue of that segment.\n\nThe batch selectors are operation granularity, not storage granularity:\neach affected row records its own waiver, so audit and\nfingerprint-invalidation semantics are identical to per-issue waiving.\nBatches skip rows already in the requested state instead of erroring.",
      "type": "object",
      "properties": {
        "documentId": {
          "description": "Scope for `ruleId`. Per-rule waivers are document-scoped — never a\nhidden project-wide exemption.",
          "type": [
            "string",
            "null"
          ]
        },
        "issueId": {
          "description": "Selector: one issue by id.",
          "type": [
            "string",
            "null"
          ]
        },
        "note": {
          "description": "Optional free-form note. Deliberately not required: an empty or\nomitted note is a perfectly valid waiver.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ruleId": {
          "description": "Selector: every issue of this rule; requires `documentId`.",
          "type": [
            "string",
            "null"
          ]
        },
        "segmentId": {
          "description": "Selector: every issue of this segment.",
          "type": [
            "string",
            "null"
          ]
        },
        "waived": {
          "description": "`true` waives open issues; `false` restores waived issues to open.",
          "type": "boolean"
        }
      },
      "required": [
        "waived"
      ]
    },
    "QaWaiveResult": {
      "type": "object",
      "properties": {
        "issues": {
          "description": "Every issue the call changed, straight from the store. Clients\nreplace their copies of these rows wholesale.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/QaIssue"
          }
        }
      },
      "required": [
        "issues"
      ]
    },
    "RpcError": {
      "type": "object",
      "properties": {
        "code": {
          "$ref": "#/$defs/RpcErrorCode"
        },
        "data": true,
        "message": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ]
    },
    "RpcErrorCode": {
      "type": "string",
      "enum": [
        "invalidRequest",
        "methodNotFound",
        "invalidParams",
        "notFound",
        "conflict",
        "filterFailed",
        "exportBlocked",
        "aiNotConfigured",
        "aiFailed",
        "io",
        "internal"
      ]
    },
    "RpcMethodCatalog": {
      "description": "Method-name-keyed catalog. Field renames must match [`methods`] constants;\nthe test below keeps them honest.",
      "type": "object",
      "properties": {
        "ai.agent.cancel": {
          "$ref": "#/$defs/MethodContract62"
        },
        "ai.agent.review": {
          "$ref": "#/$defs/MethodContract61"
        },
        "ai.agent.start": {
          "$ref": "#/$defs/MethodContract59"
        },
        "ai.agent.status": {
          "$ref": "#/$defs/MethodContract60"
        },
        "ai.assist.cancel": {
          "$ref": "#/$defs/MethodContract58"
        },
        "ai.assist.start": {
          "$ref": "#/$defs/MethodContract56"
        },
        "ai.assist.status": {
          "$ref": "#/$defs/MethodContract57"
        },
        "ai.configure": {
          "$ref": "#/$defs/MethodContract51"
        },
        "ai.harness.cancel": {
          "$ref": "#/$defs/MethodContract65"
        },
        "ai.harness.start": {
          "$ref": "#/$defs/MethodContract63"
        },
        "ai.harness.status": {
          "$ref": "#/$defs/MethodContract64"
        },
        "ai.profile.add": {
          "$ref": "#/$defs/MethodContract53"
        },
        "ai.profile.list": {
          "$ref": "#/$defs/MethodContract54"
        },
        "ai.profile.remove": {
          "$ref": "#/$defs/MethodContract55"
        },
        "ai.status": {
          "$ref": "#/$defs/MethodContract52"
        },
        "document.export": {
          "$ref": "#/$defs/MethodContract11"
        },
        "document.import": {
          "$ref": "#/$defs/MethodContract8"
        },
        "document.list": {
          "$ref": "#/$defs/MethodContract9"
        },
        "document.remove": {
          "$ref": "#/$defs/MethodContract10"
        },
        "engine.initialize": {
          "$ref": "#/$defs/MethodContract"
        },
        "engine.shutdown": {
          "$ref": "#/$defs/MethodContract2"
        },
        "memory.attach": {
          "$ref": "#/$defs/MethodContract27"
        },
        "memory.create": {
          "$ref": "#/$defs/MethodContract25"
        },
        "memory.delete": {
          "$ref": "#/$defs/MethodContract31"
        },
        "memory.detach": {
          "$ref": "#/$defs/MethodContract28"
        },
        "memory.list": {
          "$ref": "#/$defs/MethodContract26"
        },
        "memory.rename": {
          "$ref": "#/$defs/MethodContract30"
        },
        "memory.update": {
          "$ref": "#/$defs/MethodContract29"
        },
        "project.archive": {
          "$ref": "#/$defs/MethodContract7"
        },
        "project.create": {
          "$ref": "#/$defs/MethodContract3"
        },
        "project.get": {
          "$ref": "#/$defs/MethodContract5"
        },
        "project.list": {
          "$ref": "#/$defs/MethodContract4"
        },
        "project.update": {
          "$ref": "#/$defs/MethodContract6"
        },
        "qa.fix.apply": {
          "$ref": "#/$defs/MethodContract48"
        },
        "qa.fix.list": {
          "$ref": "#/$defs/MethodContract47"
        },
        "qa.list": {
          "$ref": "#/$defs/MethodContract45"
        },
        "qa.profile.get": {
          "$ref": "#/$defs/MethodContract49"
        },
        "qa.profile.update": {
          "$ref": "#/$defs/MethodContract50"
        },
        "qa.run": {
          "$ref": "#/$defs/MethodContract44"
        },
        "qa.waive": {
          "$ref": "#/$defs/MethodContract46"
        },
        "segment.confirm": {
          "$ref": "#/$defs/MethodContract16"
        },
        "segment.list": {
          "$ref": "#/$defs/MethodContract12"
        },
        "segment.lock": {
          "$ref": "#/$defs/MethodContract17"
        },
        "segment.replace": {
          "$ref": "#/$defs/MethodContract15"
        },
        "segment.update": {
          "$ref": "#/$defs/MethodContract13"
        },
        "segment.updateSource": {
          "$ref": "#/$defs/MethodContract14"
        },
        "term.add": {
          "$ref": "#/$defs/MethodContract39"
        },
        "term.delete": {
          "$ref": "#/$defs/MethodContract41"
        },
        "term.list": {
          "$ref": "#/$defs/MethodContract42"
        },
        "term.lookup": {
          "$ref": "#/$defs/MethodContract43"
        },
        "term.update": {
          "$ref": "#/$defs/MethodContract40"
        },
        "termbase.attach": {
          "$ref": "#/$defs/MethodContract34"
        },
        "termbase.create": {
          "$ref": "#/$defs/MethodContract32"
        },
        "termbase.detach": {
          "$ref": "#/$defs/MethodContract35"
        },
        "termbase.export": {
          "$ref": "#/$defs/MethodContract38"
        },
        "termbase.import": {
          "$ref": "#/$defs/MethodContract37"
        },
        "termbase.list": {
          "$ref": "#/$defs/MethodContract33"
        },
        "termbase.update": {
          "$ref": "#/$defs/MethodContract36"
        },
        "tm.delete": {
          "$ref": "#/$defs/MethodContract21"
        },
        "tm.export": {
          "$ref": "#/$defs/MethodContract23"
        },
        "tm.import": {
          "$ref": "#/$defs/MethodContract22"
        },
        "tm.list": {
          "$ref": "#/$defs/MethodContract19"
        },
        "tm.lookup": {
          "$ref": "#/$defs/MethodContract18"
        },
        "tm.pretranslate": {
          "$ref": "#/$defs/MethodContract24"
        },
        "tm.update": {
          "$ref": "#/$defs/MethodContract20"
        }
      },
      "additionalProperties": false,
      "required": [
        "engine.initialize",
        "engine.shutdown",
        "project.create",
        "project.list",
        "project.get",
        "project.update",
        "project.archive",
        "document.import",
        "document.list",
        "document.remove",
        "document.export",
        "segment.list",
        "segment.update",
        "segment.updateSource",
        "segment.replace",
        "segment.confirm",
        "segment.lock",
        "tm.lookup",
        "tm.list",
        "tm.update",
        "tm.delete",
        "tm.import",
        "tm.export",
        "tm.pretranslate",
        "memory.create",
        "memory.list",
        "memory.attach",
        "memory.detach",
        "memory.update",
        "memory.rename",
        "memory.delete",
        "termbase.create",
        "termbase.list",
        "termbase.attach",
        "termbase.detach",
        "termbase.update",
        "termbase.import",
        "termbase.export",
        "term.add",
        "term.update",
        "term.delete",
        "term.list",
        "term.lookup",
        "qa.run",
        "qa.list",
        "qa.waive",
        "qa.fix.list",
        "qa.fix.apply",
        "qa.profile.get",
        "qa.profile.update",
        "ai.configure",
        "ai.status",
        "ai.profile.add",
        "ai.profile.list",
        "ai.profile.remove",
        "ai.assist.start",
        "ai.assist.status",
        "ai.assist.cancel",
        "ai.agent.start",
        "ai.agent.status",
        "ai.agent.review",
        "ai.agent.cancel",
        "ai.harness.start",
        "ai.harness.status",
        "ai.harness.cancel"
      ]
    },
    "RpcNotification": {
      "description": "Reserved notification frame: engine-initiated, no request id, never awaited.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string"
        },
        "params": {
          "default": null
        }
      },
      "required": [
        "method"
      ]
    },
    "RpcRequest": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "method": {
          "type": "string"
        },
        "params": {
          "default": null
        }
      },
      "required": [
        "id",
        "method"
      ]
    },
    "RpcResponse": {
      "type": "object",
      "properties": {
        "error": {
          "anyOf": [
            {
              "$ref": "#/$defs/RpcError"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "result": true
      }
    },
    "Segment": {
      "type": "object",
      "properties": {
        "contextHash": {
          "type": "string"
        },
        "documentId": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "locked": {
          "description": "Locked rows are read-only: update/confirm conflict, replace,\npretranslate, propagation, and AI skip them, and QA leaves their\nissues untouched. Toggled only by `segment.lock`. Defaults false so\nrows serialized before the field existed still parse.",
          "type": "boolean",
          "default": false
        },
        "ordinal": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "origin": {
          "description": "Where the current target text came from. Absent for rows written\nbefore origins existed and for plain human typing.",
          "anyOf": [
            {
              "$ref": "#/$defs/SegmentOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sourceHash": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        },
        "state": {
          "$ref": "#/$defs/SegmentState"
        },
        "structuralPath": {
          "type": "string"
        },
        "targetText": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "documentId",
        "ordinal",
        "structuralPath",
        "sourceText",
        "targetText",
        "state",
        "revision",
        "sourceHash",
        "contextHash",
        "updatedAtMs"
      ]
    },
    "SegmentConfirmParams": {
      "type": "object",
      "properties": {
        "baseRevision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "segmentId": {
          "type": "string"
        },
        "skipTmWrite": {
          "description": "Confirm without touching translation memory: the segment still turns\nconfirmed and confirm-time QA still runs, but no TM entry is written\nand no duplicate propagation happens — the pair spreads nowhere.\nAlso the escape hatch when no writable memory is mounted. Defaults\nto false: the ordinary confirm keeps writing TM.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "segmentId",
        "baseRevision"
      ]
    },
    "SegmentConfirmResult": {
      "type": "object",
      "properties": {
        "propagated": {
          "description": "Sibling segments auto-filled from the confirmed translation.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Segment"
          }
        },
        "qaIssues": {
          "description": "Confirm-time QA: every persisted issue of the confirmed segment\nafter the engine re-ran the segment-scoped rules against the\nconfirmed text, committed in the same transaction as the confirm.\nAll statuses are included so clients can replace their records for\nthis segment wholesale. Cross-segment consistency rules are not\nre-evaluated here — those refresh on the next `qa.run`.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/QaIssue"
          }
        },
        "segment": {
          "$ref": "#/$defs/Segment"
        },
        "tmEntry": {
          "description": "The translation-memory entry written by the confirmation; absent\nwhen the confirm skipped the TM write.",
          "anyOf": [
            {
              "$ref": "#/$defs/TmEntry"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "segment",
        "propagated"
      ]
    },
    "SegmentCounts": {
      "type": "object",
      "properties": {
        "confirmed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "draft": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "openIssues": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "sourceWords": {
          "description": "Total source word count of the counted segments, computed by the\nengine with [`source_word_count`]. 口径：UAX #29 词边界；CJK 统一\n表意文字与假名逐字计 1；数字串计 1；URL/email 计 1（对齐 Crowdin\nWord Counter）。Absent from older engines — clients must render\nnothing rather than count locally.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "total": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "untranslated": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "total",
        "untranslated",
        "draft",
        "confirmed",
        "openIssues"
      ]
    },
    "SegmentListParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "limit": {
          "description": "Page size. When omitted the whole document is returned, which is the\npre-paging behavior existing clients rely on.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "offset": {
          "description": "Rows to skip in ordinal order; defaults to 0.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        }
      },
      "required": [
        "documentId"
      ]
    },
    "SegmentListResult": {
      "type": "object",
      "properties": {
        "segments": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Segment"
          }
        },
        "totalSegments": {
          "description": "Segments in the document before the page window was applied, so\nclients can size scrollbars without fetching every row.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "segments",
        "totalSegments"
      ]
    },
    "SegmentLockParams": {
      "description": "Parameters for `segment.lock`: set or clear a segment's lock. Locking is\nidempotent — locking an already-locked row (or unlocking an unlocked one)\nstill bumps the revision and succeeds. `baseRevision` follows the same\noptimistic-concurrency rule as `segment.update`.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "locked": {
          "type": "boolean"
        },
        "segmentId": {
          "type": "string"
        }
      },
      "required": [
        "segmentId",
        "locked",
        "baseRevision"
      ]
    },
    "SegmentLockResult": {
      "type": "object",
      "properties": {
        "segment": {
          "$ref": "#/$defs/Segment"
        }
      },
      "required": [
        "segment"
      ]
    },
    "SegmentOrigin": {
      "description": "Where the current target text came from, stamped by the write that put\nit there. Only writes that carry an origin stamp one — rows written\nbefore this field existed stay origin-less forever (no backfill), and an\nupdate that empties the target clears the origin (an empty target has no\norigin). Confirming never changes the origin.",
      "type": "object",
      "properties": {
        "edited": {
          "description": "Pollution signal: true once the target was edited after the origin\nwrite (Studio-style \"edited fuzzy\"). Engine-owned — the value sent\nby a client is ignored; a stamping write always resets it to false.",
          "type": "boolean",
          "default": false
        },
        "kind": {
          "$ref": "#/$defs/SegmentOriginKind"
        },
        "model": {
          "description": "Provider model that produced an `aiDraft`; absent otherwise.",
          "type": [
            "string",
            "null"
          ]
        },
        "score": {
          "description": "Real TM match score (0-100) as reported at apply time. Present only\nfor TM origins; never fabricated for AI or human writes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        }
      },
      "required": [
        "kind"
      ]
    },
    "SegmentOriginKind": {
      "description": "Closed set of places a segment's target text can honestly come from.\n`human` exists for completeness; plain human typing normally leaves the\norigin absent instead of stamping it.",
      "oneOf": [
        {
          "description": "Exact TM reuse (apply, pretranslate, confirm-time propagation).",
          "type": "string",
          "const": "tmExact"
        },
        {
          "description": "Fuzzy TM reuse; `score` carries the real match score.",
          "type": "string",
          "const": "tmFuzzy"
        },
        {
          "description": "AI-proposed draft applied by a human; `model` names the provider model.",
          "type": "string",
          "const": "aiDraft"
        },
        {
          "description": "Explicit human authorship. No engine path stamps this today; absent\nmeans the same thing.",
          "type": "string",
          "const": "human"
        }
      ]
    },
    "SegmentReplaceParams": {
      "description": "Parameters for `segment.replace`: one document-wide search-and-replace\nover target text. Matching is case-insensitive with per-character\nUnicode lowercase folding — the same semantics as the grid find box —\nand occurrences never overlap. Source text is never touched.",
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "find": {
          "description": "Text to find in target text. Must not be empty.",
          "type": "string"
        },
        "includeConfirmed": {
          "description": "Also rewrite confirmed segments. A rewritten confirmed segment moves\nback to `draft` — the confirmation covered the old text — and its TM\nentry is left as it was (replace drafts, it never confirms). Default\nfalse: confirmed matches are skipped and counted instead.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "replaceWith": {
          "description": "Replacement text. May be empty, which deletes the found text; a\ntarget emptied this way honestly returns to `untranslated`.",
          "type": "string"
        }
      },
      "required": [
        "documentId",
        "find",
        "replaceWith"
      ]
    },
    "SegmentReplaceResult": {
      "type": "object",
      "properties": {
        "demotedConfirmed": {
          "description": "How many of `segments` were confirmed before this replace moved them\nback to draft. Non-zero only with `includeConfirmed`.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "replacedOccurrences": {
          "description": "Total occurrences replaced across `segments`.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "segments": {
          "description": "Rewritten segments in grid order, carrying their new revision and\nstate, so clients can apply them without a full reload.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Segment"
          }
        },
        "skippedConfirmed": {
          "description": "Matching confirmed segments left untouched because\n`includeConfirmed` was not set.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "skippedLocked": {
          "description": "Matching locked segments left untouched. Locked rows are never\nrewritten, even with `includeConfirmed`.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        }
      },
      "required": [
        "segments",
        "replacedOccurrences",
        "demotedConfirmed",
        "skippedConfirmed"
      ]
    },
    "SegmentState": {
      "type": "string",
      "enum": [
        "untranslated",
        "draft",
        "confirmed"
      ]
    },
    "SegmentUpdateParams": {
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Optimistic concurrency: must match the segment's current revision.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "origin": {
          "description": "Where `targetText` came from, for writes that apply stored material\n(TM match apply → `tmExact`/`tmFuzzy` with the real lookup score, AI\ndraft apply → `aiDraft` with the provider model). The kinds are the\nclosed [`SegmentOrigin`] enum — nothing free-form. Omit for human\ntyping: the engine then keeps any existing origin and marks it\n`edited` when the target changed, and clears the origin entirely\nwhen the update empties the target. `origin.edited` is engine-owned\nand ignored on input.",
          "anyOf": [
            {
              "$ref": "#/$defs/SegmentOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "segmentId": {
          "type": "string"
        },
        "targetText": {
          "type": "string"
        }
      },
      "required": [
        "segmentId",
        "targetText",
        "baseRevision"
      ]
    },
    "SegmentUpdateResult": {
      "type": "object",
      "properties": {
        "segment": {
          "$ref": "#/$defs/Segment"
        }
      },
      "required": [
        "segment"
      ]
    },
    "SegmentUpdateSourceParams": {
      "description": "Parameters for `segment.updateSource`: rewrite the source text of one\nsegment (imported text carrying an OCR error or a typo the translator\nmust fix in place). Guards mirror `segment.update`: a stale\n`baseRevision` conflicts and a locked segment conflicts. The source\nmust not become empty — a segment without source text has no meaning.\nRewriting the source of a confirmed segment honestly returns it to\n`draft`: the confirmation covered the old source. Any TM entry written\nby an earlier confirm is left as it was (mirroring `segment.replace`),\nand the stored target-origin stamp is kept — it still describes where\nthe target text came from.",
      "type": "object",
      "properties": {
        "baseRevision": {
          "description": "Optimistic concurrency: must match the segment's current revision.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "segmentId": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        }
      },
      "required": [
        "segmentId",
        "sourceText",
        "baseRevision"
      ]
    },
    "SegmentUpdateSourceResult": {
      "type": "object",
      "properties": {
        "segment": {
          "$ref": "#/$defs/Segment"
        }
      },
      "required": [
        "segment"
      ]
    },
    "ShutdownResult": {
      "type": "object",
      "properties": {
        "ok": {
          "type": "boolean"
        }
      },
      "required": [
        "ok"
      ]
    },
    "TagIntegrityReport": {
      "description": "Verdict on whether a proposal preserves the source's placeholder tokens.\n\n`missing` lists tokens the proposal dropped, `extra` lists tokens it\ninvented; both are multiset differences, so a duplicated token counts.\nDetection is [`tl_domain::placeholder_mismatch`] — the same detector the\ndeterministic QA tag rules use, so the AI gate and QA agree token for\ntoken.",
      "type": "object",
      "properties": {
        "extra": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "missing": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "ok": {
          "type": "boolean"
        }
      },
      "required": [
        "ok"
      ]
    },
    "TaskPackageProjectReference": {
      "type": "object",
      "properties": {
        "instructions": {
          "type": "string",
          "default": ""
        },
        "originProjectId": {
          "type": "string"
        },
        "packageId": {
          "type": "string"
        },
        "parentPackageId": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "packageId",
        "originProjectId"
      ]
    },
    "TermAddParams": {
      "type": "object",
      "properties": {
        "definition": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "domain": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "forbidden": {
          "description": "Marks the target as forbidden instead of preferred.",
          "type": "boolean",
          "default": false
        },
        "sourceTerm": {
          "type": "string"
        },
        "targetLocale": {
          "type": "string"
        },
        "targetTerm": {
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "termbaseId",
        "sourceTerm",
        "targetTerm",
        "targetLocale"
      ]
    },
    "TermAddResult": {
      "type": "object",
      "properties": {
        "entry": {
          "$ref": "#/$defs/TermEntry"
        }
      },
      "required": [
        "entry"
      ]
    },
    "TermDeleteParams": {
      "type": "object",
      "properties": {
        "entryId": {
          "type": "string"
        },
        "translationId": {
          "description": "When set, removes only this translation and keeps the entry.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "entryId"
      ]
    },
    "TermDeleteResult": {
      "type": "object",
      "properties": {
        "entry": {
          "description": "The surviving entry after a translation-level delete; `None` when the\nwhole entry was removed.",
          "anyOf": [
            {
              "$ref": "#/$defs/TermEntry"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "TermEntry": {
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "definition": {
          "type": [
            "string",
            "null"
          ]
        },
        "domain": {
          "type": [
            "string",
            "null"
          ]
        },
        "example": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "partOfSpeech": {
          "type": [
            "string",
            "null"
          ]
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sourceLocale": {
          "type": "string"
        },
        "sourceTerm": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/TermStatus"
        },
        "termbaseId": {
          "type": "string"
        },
        "translations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermTranslation"
          }
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "termbaseId",
        "sourceLocale",
        "sourceTerm",
        "status",
        "revision",
        "translations",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "TermExchangeFormat": {
      "type": "string",
      "enum": [
        "csv",
        "tsv",
        "tbx"
      ]
    },
    "TermListParams": {
      "type": "object",
      "properties": {
        "limit": {
          "description": "Page size. When omitted every entry from `offset` on is returned,\nwhich is the pre-paging behavior existing clients rely on.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "offset": {
          "description": "Entries to skip in source-term order; defaults to 0.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "termbaseId"
      ]
    },
    "TermListResult": {
      "type": "object",
      "properties": {
        "entries": {
          "description": "One window of entries in source-term order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermEntry"
          }
        },
        "total": {
          "description": "Entries in the termbase before the page window was applied, so\nclients can page honestly.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "entries",
        "total"
      ]
    },
    "TermLookupParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "sourceText"
      ]
    },
    "TermLookupResult": {
      "type": "object",
      "properties": {
        "matches": {
          "description": "Hits over the normalized source text, ordered by span position. Spans\nare Unicode-scalar offsets into the normalized text.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermMatch"
          }
        }
      },
      "required": [
        "matches"
      ]
    },
    "TermMatch": {
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "entryId": {
          "type": "string"
        },
        "sourceTerm": {
          "type": "string"
        },
        "start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "termbaseId": {
          "type": "string"
        },
        "translations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermTranslation"
          }
        }
      },
      "required": [
        "termbaseId",
        "entryId",
        "sourceTerm",
        "translations",
        "start",
        "end"
      ]
    },
    "TermStatus": {
      "type": "string",
      "enum": [
        "candidate",
        "active",
        "deprecated"
      ]
    },
    "TermTranslation": {
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "entryId": {
          "type": "string"
        },
        "forbidden": {
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "locale": {
          "type": "string"
        },
        "preferred": {
          "type": "boolean"
        },
        "term": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "id",
        "entryId",
        "locale",
        "term",
        "preferred",
        "forbidden",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "TermUpdateParams": {
      "type": "object",
      "properties": {
        "entryId": {
          "type": "string"
        },
        "forbidden": {
          "description": "Marks the selected translation as forbidden (or preferred again).",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "sourceTerm": {
          "description": "New source term for the entry. Left unchanged when omitted.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "targetTerm": {
          "description": "New term text for the selected translation.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "translationId": {
          "description": "Translation being edited. Required for `target_term` / `forbidden`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "entryId"
      ]
    },
    "TermUpdateResult": {
      "type": "object",
      "properties": {
        "entry": {
          "$ref": "#/$defs/TermEntry"
        }
      },
      "required": [
        "entry"
      ]
    },
    "Termbase": {
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "domain": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sourceLocale": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "writable": {
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "name",
        "sourceLocale",
        "writable",
        "revision",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "TermbaseAttachParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "termbaseId"
      ]
    },
    "TermbaseAttachResult": {
      "type": "object",
      "properties": {
        "mount": {
          "$ref": "#/$defs/TermbaseMount"
        }
      },
      "required": [
        "mount"
      ]
    },
    "TermbaseCreateParams": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "sourceLocale": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "sourceLocale"
      ]
    },
    "TermbaseDetachParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "termbaseId"
      ]
    },
    "TermbaseDetachResult": {
      "description": "Carries the removed mount. Detaching a termbase that is not attached fails\nwith `notFound` instead of pretending success.",
      "type": "object",
      "properties": {
        "mount": {
          "$ref": "#/$defs/TermbaseMount"
        }
      },
      "required": [
        "mount"
      ]
    },
    "TermbaseExportParams": {
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/$defs/TermExchangeFormat"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "overwrite": {
          "description": "Replace an existing destination file (staged sibling temp + atomic\nrename). Defaults to false: the export is refused with `exportBlocked`\nwhen the path exists. Even with overwrite, the engine refuses paths\ninside its own managed data directory.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "path": {
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "termbaseId",
        "path"
      ]
    },
    "TermbaseExportResult": {
      "type": "object",
      "properties": {
        "exported": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "outputPath": {
          "type": "string"
        }
      },
      "required": [
        "outputPath",
        "exported"
      ]
    },
    "TermbaseImportParams": {
      "type": "object",
      "properties": {
        "format": {
          "description": "Explicit exchange format. When omitted, inferred from the extension.",
          "anyOf": [
            {
              "$ref": "#/$defs/TermExchangeFormat"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "path": {
          "type": "string"
        },
        "targetLocale": {
          "description": "Fallback target locale for rows/entries that do not carry one.",
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        }
      },
      "required": [
        "termbaseId",
        "path",
        "targetLocale"
      ]
    },
    "TermbaseImportResult": {
      "type": "object",
      "properties": {
        "added": {
          "description": "New term entries created.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "imported": {
          "description": "Entries read from the file.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "merged": {
          "description": "Existing entries that gained or refreshed translations.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "imported",
        "added",
        "merged"
      ]
    },
    "TermbaseListParams": {
      "type": "object",
      "properties": {
        "projectId": {
          "description": "When set, `mounts` is restricted to this project.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "TermbaseListResult": {
      "type": "object",
      "properties": {
        "mounts": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermbaseMount"
          }
        },
        "termbases": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Termbase"
          }
        }
      },
      "required": [
        "termbases",
        "mounts"
      ]
    },
    "TermbaseMount": {
      "type": "object",
      "properties": {
        "createdAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "enabled": {
          "type": "boolean"
        },
        "priority": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "revision": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "termbaseId": {
          "type": "string"
        },
        "updatedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "writable": {
          "type": "boolean"
        }
      },
      "required": [
        "projectId",
        "termbaseId",
        "priority",
        "writable",
        "enabled",
        "revision",
        "createdAtMs",
        "updatedAtMs"
      ]
    },
    "TermbaseUpdateParams": {
      "description": "`termbase.update` — edit one mount: enable/disable the read path\n(`term.lookup` and QA only consult enabled mounts), flip the per-mount\nwrite switch, and/or move the mount to a new priority position. Omitted\nfields stay unchanged; an all-omitted update is `invalidParams`.\n\nUnlike TM mounts there is no single-writable invariant: `termbase.attach`\nmounts every termbase writable, several writable mounts are the normal\nstate, and `writable` here is a per-mount switch. Term additions target\nthe first writable mount in priority order.",
      "type": "object",
      "properties": {
        "enabled": {
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "priority": {
          "description": "Target position in the project's mount list (0 = highest priority).\nValues past the end clamp to the last position. Sibling mounts are\nrenumbered to keep priorities contiguous.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "termbaseId": {
          "type": "string"
        },
        "writable": {
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "projectId",
        "termbaseId"
      ]
    },
    "TermbaseUpdateResult": {
      "description": "The project's mounts after the edit, in priority order — a priority\nmove renumbers siblings, so one mount alone would hide real changes.",
      "type": "object",
      "properties": {
        "mounts": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TermbaseMount"
          }
        }
      },
      "required": [
        "mounts"
      ]
    },
    "TmDeleteParams": {
      "type": "object",
      "properties": {
        "entryId": {
          "type": "string"
        }
      },
      "required": [
        "entryId"
      ]
    },
    "TmDeleteResult": {
      "type": "object",
      "properties": {
        "entry": {
          "description": "The removed entry, echoed so clients can report what was deleted.",
          "$ref": "#/$defs/TmEntry"
        }
      },
      "required": [
        "entry"
      ]
    },
    "TmEntry": {
      "type": "object",
      "properties": {
        "confirmedAtMs": {
          "type": "integer",
          "format": "int64"
        },
        "id": {
          "type": "string"
        },
        "memoryId": {
          "type": "string"
        },
        "originDocumentId": {
          "type": "string"
        },
        "originProjectId": {
          "type": "string"
        },
        "originSegmentId": {
          "type": "string"
        },
        "sourceHash": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        },
        "targetText": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "memoryId",
        "sourceText",
        "targetText",
        "sourceHash",
        "originProjectId",
        "originDocumentId",
        "originSegmentId",
        "confirmedAtMs"
      ]
    },
    "TmExchangeFormat": {
      "type": "string",
      "enum": [
        "tmx",
        "csv",
        "tsv"
      ]
    },
    "TmExportParams": {
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/$defs/TmExchangeFormat"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "memoryId": {
          "description": "Memory to export. Must be mounted on the project. Defaults to the\nproject's writable mount; fails with `conflict` when the project has\nno writable mount and no explicit id was given.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "overwrite": {
          "description": "Replace an existing destination file (staged sibling temp + atomic\nrename). Defaults to false: the export is refused with `exportBlocked`\nwhen the path exists. Even with overwrite, the engine refuses paths\ninside its own managed data directory.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "path": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "path"
      ]
    },
    "TmExportResult": {
      "type": "object",
      "properties": {
        "exported": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "outputPath": {
          "type": "string"
        }
      },
      "required": [
        "outputPath",
        "exported"
      ]
    },
    "TmImportParams": {
      "type": "object",
      "properties": {
        "format": {
          "description": "Explicit exchange format. When omitted, inferred from the extension.",
          "anyOf": [
            {
              "$ref": "#/$defs/TmExchangeFormat"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "memoryId": {
          "description": "Destination memory. Must be mounted on the project. Defaults to the\nproject's writable mount; fails with `conflict` when the project has\nno writable mount and no explicit id was given.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "path": {
          "type": "string"
        },
        "projectId": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "path"
      ]
    },
    "TmImportResult": {
      "type": "object",
      "properties": {
        "added": {
          "description": "New TM entries created.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "imported": {
          "description": "Units read from the file.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "updated": {
          "description": "Existing entries whose target was replaced.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "imported",
        "added",
        "updated"
      ]
    },
    "TmListParams": {
      "type": "object",
      "properties": {
        "limit": {
          "description": "Page size (1..=[`TM_LIST_MAX_LIMIT`]); defaults to\n[`TM_LIST_DEFAULT_LIMIT`].",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "memoryId": {
          "description": "Memory to page. Must be mounted on the project (enabled or not —\nmanaging a disabled mount's entries is legitimate). Defaults to the\nproject's writable mount; fails with `conflict` when the project has\nno writable mount and no explicit id was given.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "offset": {
          "description": "Entries to skip before the page starts; defaults to 0.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "query": {
          "description": "Case-insensitive substring filter over source and target text.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "projectId"
      ]
    },
    "TmListResult": {
      "type": "object",
      "properties": {
        "entries": {
          "description": "One page of entries, most recently confirmed first.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/TmEntry"
          }
        },
        "total": {
          "description": "Entries that matched the filter before `offset`/`limit`, so clients\ncan page honestly.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "entries",
        "total"
      ]
    },
    "TmLookupParams": {
      "type": "object",
      "properties": {
        "limit": {
          "description": "Maximum matches to return; defaults to [`TM_LOOKUP_DEFAULT_LIMIT`].",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "default": null,
          "minimum": 0
        },
        "minScore": {
          "description": "Fuzzy score floor (1..=100); defaults to\n[`TM_LOOKUP_DEFAULT_MIN_SCORE`]. Exact matches always pass.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "default": null,
          "maximum": 255,
          "minimum": 0
        },
        "projectId": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        }
      },
      "required": [
        "projectId",
        "sourceText"
      ]
    },
    "TmLookupResult": {
      "type": "object",
      "properties": {
        "matches": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TmMatchItem"
          }
        },
        "totalMatches": {
          "description": "Total candidates that met the floor before the limit was applied, so\nclients can tell when a `limit` cut the list short.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "matches",
        "totalMatches"
      ]
    },
    "TmMatchGrade": {
      "oneOf": [
        {
          "description": "Normalized source text matches exactly.",
          "type": "string",
          "const": "exact"
        },
        {
          "description": "Exact source and matching neighbour context. Reserved for later phases.",
          "type": "string",
          "const": "inContext"
        },
        {
          "description": "Recalled by similarity; score reflects the fuzzy match quality.",
          "type": "string",
          "const": "fuzzy"
        }
      ]
    },
    "TmMatchItem": {
      "type": "object",
      "properties": {
        "entry": {
          "$ref": "#/$defs/TmEntry"
        },
        "grade": {
          "$ref": "#/$defs/TmMatchGrade"
        },
        "memoryName": {
          "description": "Name of the memory the entry lives in (`entry.memoryId` carries the\nid). `None` only when the memory row is unknown to the engine.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "score": {
          "description": "0..=100.",
          "type": "integer",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        }
      },
      "required": [
        "entry",
        "score",
        "grade"
      ]
    },
    "TmPretranslateParams": {
      "type": "object",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "minScore": {
          "description": "Score threshold (1..=100) a match must reach to be applied; defaults\nto [`TM_PRETRANSLATE_DEFAULT_MIN_SCORE`].",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "default": null,
          "maximum": 255,
          "minimum": 0
        }
      },
      "required": [
        "documentId"
      ]
    },
    "TmPretranslateResult": {
      "type": "object",
      "properties": {
        "checked": {
          "description": "Untranslated segments examined.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "exact": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "fuzzy": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "pretranslated": {
          "description": "Segments filled from the TM (exact + fuzzy).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "segments": {
          "description": "The segments that changed, at their new revisions.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Segment"
          }
        },
        "skippedLocked": {
          "description": "Untranslated segments left alone because they are locked. Not part\nof `checked`.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        }
      },
      "required": [
        "checked",
        "pretranslated",
        "exact",
        "fuzzy",
        "segments"
      ]
    },
    "TmUpdateParams": {
      "type": "object",
      "properties": {
        "entryId": {
          "type": "string"
        },
        "sourceText": {
          "type": "string"
        },
        "targetText": {
          "type": "string"
        }
      },
      "required": [
        "entryId",
        "sourceText",
        "targetText"
      ]
    },
    "TmUpdateResult": {
      "type": "object",
      "properties": {
        "entry": {
          "$ref": "#/$defs/TmEntry"
        }
      },
      "required": [
        "entry"
      ]
    }
  }
}